既然你已经明确了无前缀路由 (/:slug)、纯文本内容以及纯后端 API 模式，这套提示词将专注于构建一个高性能、权限严谨的 Rust 服务。

你可以直接将以下模块化的提示词（Prompt）依次复制给 AI（如 GPT-4, Claude 3.5 等），它们将引导 AI 为你生成完整的生产级代码。

阶段 1：基础设施与容器化 (Infrastructure)
文件名：prompt_01_setup.md

提示词内容： 我需要初始化一个名为 article-recorder 的 Rust 后端项目。

技术规格：

框架： Axum + Tokio。

数据库： MySQL 8.0，使用 sqlx (支持编译时类型检查)。

容器化： > * 提供 Dockerfile：采用多阶段构建，第一阶段使用 rust:1.75 编译，第二阶段使用 debian:bookworm-slim 运行，确保镜像体积在 50MB 左右。

提供 docker-compose.yml：包含 app 和 mysql 两个服务，自动挂载初始化 SQL 脚本。

CI/CD： 提供 .github/workflows/deploy.yml，推送到 main 时自动打包镜像至 GHCR。

请输出： 完整的目录结构、Cargo.toml、Dockerfile、docker-compose.yml 以及包含数据库连接字符串的 .env 模板。